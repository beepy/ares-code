#include <Dialogs.h>#ifndef __CONDITIONALMACROS__#include "ConditionalMacros.h"#endif // __CONDITIONALMACROS__#if TARGET_OS_WIN32	#ifndef __QUICKTIMEVR__	#include <QuickTimeVR.h>	#endif	#ifndef __QTUtilities__	#include "QTUtilities.h"	#endif	#ifndef __QTVRUtilities__	#include "QTVRUtilities.h"	#endif	#include <TextUtils.h>	#include <Script.h>	#include <string.h>#endif // TARGET_OS_WIN32#include "Resources.h"#include "Dialog Font.h"/* Also stolen from an unknown source	** Make sure you have the port correctly set to the dialog window	before you call this, otherwise the system font could get messed	up.*/void	SetDialogFontAndSize( DialogPtr theDialog, short fontNo, short fontSize ){	FontInfo	fInfo;	TextFont( fontNo );	TextSize( fontSize );	GetFontInfo( &fInfo );		(**(((DialogPeek)theDialog)->textH)).txFont = fontNo;	(**(((DialogPeek)theDialog)->textH)).txSize = fontSize;		(**(((DialogPeek)theDialog)->textH)).lineHeight = fInfo.ascent + fInfo.descent + fInfo.leading;	(**(((DialogPeek)theDialog)->textH)).fontAscent = fInfo.ascent;}